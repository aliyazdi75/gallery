import 'request_fullscreen_interface.dart';

RequestFullscreen getRequestFullscreen({Function? onEnter, Function? onExit}) =>
    throw UnsupportedError(
        'Cannot request fullscreen without the package dart:html');
